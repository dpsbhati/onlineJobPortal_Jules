export enum job_type_post {
    Postnow = 'Post Now',
    Schedulelater = 'Schedule Later'
}

export enum jobpost_status {
    Draft = 'Draft',
    Posted = 'Posted'
}

export enum job_opening {
    Hold = 'Hold',   
    Active = 'Active',
    Close = 'Close'
}           